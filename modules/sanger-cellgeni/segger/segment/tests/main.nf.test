nextflow_process {

    name "Test Process SEGGER_SEGMENT"
    script "../main.nf"
    process "SEGGER_SEGMENT"

    tag "modules"
    tag "modules_sanger-cellgeni"
    tag "segger"
    tag "segger/segment"

    test("xenium - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [ id:'test' ],
                    file(params.modules_testdata_base_path + 'spatial/xenium/sample/', checkIfExists: true),
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
