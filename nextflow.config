// Root-level nextflow.config loaded automatically when nf-test is invoked from the
// repository root. Pulls in the shared test settings (S3 endpoint, testdata path).
includeConfig "tests/config/nextflow.config"
